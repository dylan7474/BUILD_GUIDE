sudo tee /usr/local/bin/snapshot-help >/dev/null <<'SH'
#!/usr/bin/env bash
cat <<'EOF'

📘  SNAPSHOT MAINTENANCE CHEAT SHEET
───────────────────────────────────────────────────────────
Your system uses Btrfs + Snapper + grub-btrfs integration.

Snapshots are stored in /.snapshots and appear in GRUB
under “Ubuntu Snapshots” as read-only boot entries.

───────────────────────────────────────────────────────────
🧱 CREATE A SNAPSHOT
───────────────────────────────────────────────────────────
  snapshot "Description of what you’re doing"
      → Creates a new snapshot and refreshes GRUB.
        Example: snapshot "Before NVIDIA driver install"

───────────────────────────────────────────────────────────
🧾 LIST SNAPSHOTS
───────────────────────────────────────────────────────────
  snapshot-list
      → Shows all snapshots with ID, type, date, and description.

───────────────────────────────────────────────────────────
⭐ MARK IMPORTANT
───────────────────────────────────────────────────────────
  snapshot-important <ID>
      → Marks snapshot so cleanup won’t delete it.
        Example: snapshot-important 5

───────────────────────────────────────────────────────────
🧹 CLEAN OLD SNAPSHOTS
───────────────────────────────────────────────────────────
  snapshot-clean
      → Enforces your number-based cleanup policy.
        Keeps last 10 snapshots (and up to 5 marked important).

───────────────────────────────────────────────────────────
🗑️ DELETE SPECIFIC SNAPSHOT
───────────────────────────────────────────────────────────
  snapshot-rm <ID>
      → Deletes one snapshot and refreshes GRUB.

───────────────────────────────────────────────────────────
💾 TAKE SNAPSHOT + UPGRADE SYSTEM
───────────────────────────────────────────────────────────
  pre-update
      → Takes a timestamped snapshot, then runs apt update/upgrade.

───────────────────────────────────────────────────────────
🔁 ROLL BACK TO A SNAPSHOT
───────────────────────────────────────────────────────────
  sudo snapper -c root rollback <ID>
  sudo reboot
      → Makes the chosen snapshot your active root filesystem.

───────────────────────────────────────────────────────────
🔍 VIEW IN GRUB MENU
───────────────────────────────────────────────────────────
Reboot → Advanced options → “Ubuntu Snapshots”.
These entries boot read-only snapshots safely.

───────────────────────────────────────────────────────────
💡 TIPS
───────────────────────────────────────────────────────────
• Keep one “Golden image – fully configured” snapshot:
      snapshot "Golden image – fully configured"
      ID=\$(snapper -c root list | awk '/Golden image/{print \$1}' | tail -n1)
      sudo snapper -c root modify --userdata "important=yes" "\$ID"

• Temporarily adjust cleanup limits for testing:
      sudo sed -i 's/^NUMBER_LIMIT=.*/NUMBER_LIMIT="5"/' /etc/snapper/configs/root
      sudo sed -i 's/^NUMBER_MIN_AGE=.*/NUMBER_MIN_AGE="0"/' /etc/snapper/configs/root

───────────────────────────────────────────────────────────
EOF
SH
sudo chmod +x /usr/local/bin/snapshot-help
